<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:orientation="vertical" >

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:orientation="horizontal" >

        <ImageView
            android:id="@+id/icon_subtract"
            android:layout_width="36dp"
            android:layout_height="31dp"
            android:background="@drawable/button_shoppingcart_reduction_off" />

        <EditText
            android:id="@+id/goods_count"
            android:layout_width="36dp"
            android:layout_height="31dp"
            android:background="@drawable/bg_shoppingcart_goodsnum_edit"
            android:gravity="center"
            android:inputType="number"
            android:maxLength="2"
            android:text="1"
            android:textColor="#000000"
            android:textSize="20sp" >
        </EditText>

        <ImageView
            android:id="@+id/icon_add"
            android:layout_width="36dp"
            android:layout_height="31dp"
            android:background="@drawable/button_shoppingcart_plus_on" />
    </LinearLayout>

    <TextView 
        	android:id="@+id/remain"
        	android:layout_width="wrap_content"
        	android:layout_height="wrap_content"
        	android:text=" 库存剩余25件"
        	android:drawableLeft="@drawable/img_goodsnum_hint"
        	android:textColor="#ff0000"
        	android:textSize="11dp"
        	android:layout_gravity="right"
        	android:layout_marginTop="8dp"
        	android:visibility="gone"
        />
    
</LinearLayout>